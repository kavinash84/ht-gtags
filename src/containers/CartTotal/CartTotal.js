import React, { Component } from 'react';
import CartTotalContainer from 'components/CartTotal';

export default class CartTotal extends Component {
  render() {
    // const styles = require('./CartTotal.scss');

    return <CartTotalContainer />;
  }
}
