import React, { Component } from 'react';
import TopSellingProductsWrapper from 'components/TopSellingProducts';

export default class TopSellingProducts extends Component {
  render() {
    return (
      <section>
        <TopSellingProductsWrapper />
      </section>
    );
  }
}
