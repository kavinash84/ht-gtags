import React, { Component } from 'react';
import RecentlyViewedProductsWrapper from 'components/RecentlyViewedProducts';

export default class RecentlyViewedProducts extends Component {
  render() {
    return (
      <section>
        <RecentlyViewedProductsWrapper />
      </section>
    );
  }
}
